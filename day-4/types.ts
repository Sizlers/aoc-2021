
export type boardItem = {
  row: number, 
  column: number, 
  board: number, 
  number: number 
}

export type boardSum = {
  board: number, 
  sum: number
}

export type hits = {
  id: string, 
  score: number
}